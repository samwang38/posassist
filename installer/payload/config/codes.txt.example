# PosAssist 自訂結帳代碼
# 一行一筆：分類|名稱|代碼
# 檔案順序就是面板上的顯示順序；分類依首次出現的先後排列。
# 三個欄位都不能含「|」或換行，名稱與代碼不能空白。
#
# 複製成同目錄的 codes.txt 就會生效，或直接用面板右上角的「編輯」新增。
# 面板存檔時會把前一版留成 codes.txt.bak。
#
# 名稱建議 5 個字以內，4 欄的格子才放得下一行。
#
# 分類可以再分一層子分類，寫成「主分類/子分類」，例如 配件/袋類。
# 主分類是上面那列頁籤，子分類會在頁籤底下分段、每段一個小標；不寫就不分段。
# 子分類只有一層，不能寫成 配件/袋類/紙袋。
#
# 要把常按的代碼固定在面板最上面（不受切換分類影響），
# 在面板上對那顆按鈕按右鍵選「釘選到最上面」，或在「編輯」裡勾「釘選」。
# 釘選清單存在同目錄的 codes.pins.txt，一行一個代碼，這個檔不會被動到。

常用|環保紙袋|07310011
常用|大提袋|07310012
配件/袋類|紙提袋|07310013
配件/保護|保護貼|07320145
服務|全機包膜|07320200
